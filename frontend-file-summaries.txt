{
  "frontend/src/index.css": "Global styles with OKLCH color system; darkened `--foreground` to L=0.12, `--muted-foreground` to L=0.38, and `--secondary-foreground` to L=0.12 for stronger contrast on light backgrounds across all sections.",
  "frontend/tailwind.config.js": "Extended Tailwind config with updated `ink` color palette — `ink.DEFAULT` darkened to L=0.12, `ink.light` to L=0.25, and `ink.muted` to L=0.38 — ensuring all body text, descriptions, and secondary text meet WCAG AA contrast ratios on white/off-white backgrounds.",
  "frontend/src/App.tsx": "Root app component composing the single-page layout with a light snow background, auto-showing the CustomizationPrompt dialog 800ms after load.",
  "frontend/src/components/Navigation.tsx": "Sticky navigation bar with nav link weight bumped to `font-semibold` for improved legibility against the white background on both desktop and mobile menus.",
  "frontend/src/components/Hero.tsx": "Full-screen hero section with a strengthened `bg-snow/88` overlay for crisper dark text contrast, `font-medium` added to tagline and stat labels, and border opacity on the phone CTA button increased for better visibility.",
  "frontend/src/components/Services.tsx": "Services grid section with icon color classes darkened (e.g., `text-blue-600`, `text-amber-600`, `text-green-700`) and `font-medium` added to card descriptions and the bottom CTA paragraph for stronger contrast.",
  "frontend/src/components/About.tsx": "About section with `font-medium` applied to all body paragraphs, blockquote, and highlight card descriptions; trust badge borders darkened to `border-orange/60` and quote mark opacity raised to 60% for better readability.",
  "frontend/src/components/Contact.tsx": "Contact section with form labels upgraded to `font-bold`, description text changed to `text-ink-light font-medium`, service area note border darkened to `border-orange/60` with `font-semibold` text, and contact info panel values using `font-medium` for clear legibility.",
  "frontend/src/components/Footer.tsx": "Multi-column footer with all secondary text upgraded from `text-ink-muted` to `text-ink-light font-medium`, section headings using `font-bold`, link list items using `font-medium`, and bottom bar copyright text using `text-ink-light` for consistent readability.",
  "frontend/src/components/CustomizationPrompt.tsx": "A Dialog modal that prompts the user to describe specific website changes they want, featuring quick-pick category chips (Colors, Business Name, Layout, Services, Contact Info) and a free-text textarea for detailed instructions."
}